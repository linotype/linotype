{
  "name": "@linotype/nuxt-playground",
  "private": true,
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "update": "npx taze major",
    "test": "echo \"Error: no test specified\" && exit 1",
    "analyze": "nuxi analyze",
    "build": "nuxi build",
    "dev": "export NODE_TLS_REJECT_UNAUTHORIZED=0 && nuxi dev",
    "generate": "nuxi generate",
    "preview": "nuxi build && nuxi preview",
    "start": "node .output/server/index.mjs"
  },
  "devDependencies": {
    "@unocss/nuxt": "^0.50.7",
    "nuxt": "3.3.3"
  }
}
